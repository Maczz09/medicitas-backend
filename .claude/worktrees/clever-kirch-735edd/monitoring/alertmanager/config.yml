route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
  receiver: 'dummy-webhook'

receivers:
  - name: 'dummy-webhook'
    webhook_configs:
      - url: 'http://backend:3000/api/webhook/alertmanager-dummy'
        send_resolved: true
