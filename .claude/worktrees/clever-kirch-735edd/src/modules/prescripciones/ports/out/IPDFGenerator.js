class IPDFGenerator {
  /**
   * Genera el PDF de la receta de contingencia y lo guarda en el filesystem.
   * @returns {Promise<{ rutaPdf: string, urlDescarga: string }>}
   */
  async generar(receta) { throw new Error('No implementado'); }
}

module.exports = { IPDFGenerator };
